# Checkpoint 20260725_002 — Phase 7 CLOSED (после ручного теста и доработок)

> **Date:** 2026-07-25
> **Branch:** dev
> **Last commit:** d817e0b docs(backlog): add GEXF export and per-row dropdown
> **Current phase:** Phase 7 (GUI: панель действий) — CLOSED (ручной тест пройден)
> **Next:** Phase 8 — overload resolution in edge detection (BACKLOG P1)

---

## Delta относительно 20260725_001

Phase 7 закрыта после ручного тестирования пользователем:

| Что | Коммит |
|-----|--------|
| Hotfix: панель не разблокировалась после выполнения — PySide6 держит слабую ссылку на лямбду в `finished` (GC); worker без сильной ссылки. Фикс: bound-метод `_on_worker_finished` + `_active_workers` | `ac13a0b` |
| Hotfix: OK/Отмена были в середине диалогов — `BaseActionDialog` добавлял button box до полей. Фикс: `_add_buttons()` последним | `ac13a0b` |
| Регрессионные offscreen-тесты (`test_action_panel_smoke.py`, 5 шт.) | `ac13a0b`, `150c8a3` |
| Feature: опциональный каталог экспорта графа (`output_dir` в `graph_prepare`; пусто = `<codebase>/.dbm_graph/`); автодефолты панели — только в `required_fields` | `150c8a3` |
| Уроки §42 (PySide6 lambda GC), §43 (кнопки диалога последними) | `a7cd157` |
| `Phase_07.md` обновлён, BACKLOG += GEXF / per-row dropdown (P3) | `aef7011`, `d817e0b` |

Всё остальное (архитектура, реестр действий, `gui_settings.json`, CLI-копирование,
Gephi через graphml) — см. `20260725_001_checkpoint.md`, остаётся в силе.

---

## Test status

```bash
unset SSL_CERT_FILE REQUESTS_CA_BUNDLE CURL_CA_BUNDLE && uv run pytest tests/unit/ -q
# 307 passed
uv run ruff check src/ tests/
# All checks passed!
```

- Baseline до Phase 7: 278; итого после фазы: **307** (gui_settings 8,
  action_cli 10, action_registry 6, action_panel_smoke 5).
- Ручной тест GUI пользователем — пройден (замечания закрыты hotfix'ом).

---

## Known gaps / NOT done (актуальные)

- **Overload resolution in edge detection** — → Phase 8 (BACKLOG P1).
- **GEXF export**, **per-row dropdown** — BACKLOG P3 (со ссылками на Phase 7).
- **AST qualify-refs**, **pin extension versions**, **устаревший roadmap**,
  **flaky env-тест** — BACKLOG P3/P3+ без изменений.

---

## Where to read more

| Doc | Why |
|-----|-----|
| `-=PHASES=-/Phase_07.md` | Итоговый свод фазы (включая hotfix и доработки) |
| `-=tasks=-/phase_07/002_result_phase_07.md` §3.1-3.2 | Hotfix и output_dir — причины и фиксы |
| `LESSONS_LEARNED.md` §39-43 | Все уроки Phase 7 |
| `-=CHECKPOINTS=-/20260725_001_checkpoint.md` | Детальный снапшот архитектуры Phase 7 |

---

## Next planned work

**Phase 8 — Overload resolution in edge detection** (BACKLOG P1):
`_build_names_index` индексирует по bare `object_name`; вызовы перегруженных
функций ведут к первой попавшейся вершине. MVP: литералы + колонки с известным
типом; неразрешимые вызовы — как сейчас + warning. Чеклист — `LESSONS_LEARNED.md`
«Контрольный список для Phase 8».

Альтернативы: real-DB deploy validate re-run против `qr_pamyat`; schema-only mode.
