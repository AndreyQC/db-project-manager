# Checkpoint 20260730_001 — Phase 9 fully done (compare CLI + GUI)

> **Date:** 2026-07-30
> **Branch:** dev
> **Last commit:** `1d261db fix(gui): allow picking a directory without a connection in compare`
> **Current phase:** Phase 9 (compare) — fully done (CLI + GUI), ручной тест пройден
> **Next:** Phase 8 — overload resolution in edge detection (BACKLOG P1)

---

## Delta относительно 20260729_001

Phase 9 закрыта полностью: GUI action для compare добавлен в реестр Phase 7 и
прошёл ручной тест пользователем.

| Что | Коммит |
|-----|--------|
| GUI action: 4-е действие `compare` (`CompareSettings`, `build_cli_compare`, `CompareDialog`, `CompareWorker`, `_side_spec`, запись в `ACTIONS`, метки, ветка в `_on_action_finished`) | `c26ecd7` |
| BACKLOG: «GUI action для compare» помечен ВЫПОЛНЕНО | `22f783c` |
| Hotfix (ручной тест): плейсхолдер `(каталог вместо подключения)` в combo — выбор каталога без нарушения XOR | `1d261db` |
| `_final` дизайн + `result` GUI-задачи | `6fafe9a`, `20260729_003` |

Compare теперь доступен из GUI: dropdown → «Сравнить состояния (БД или каталог
reverse-engineer)» → «Настроить…» (2 combo подключений с плейсхолдером + 2 строки
каталога + output + keep_model_dir) → «Выполнить». После выполнения — каталог отчёта
открывается в viewer, в статусе сводка (added/removed/changed/unchanged).

---

## Architecture map (актуальная, дополнения Phase 9)

| Concern | Path | Note |
|---------|------|------|
| Diff domain models | `src/db_project_manager/domain/diff.py` | `CodebaseManifest`, snapshots, `DiffReport` |
| Diff infra | `src/db_project_manager/infrastructure/diff/` | `normalize_sql.py` (sqlglot), `snapshot.py`, `comparator.py` |
| Compare orchestration | `src/db_project_manager/application/compare_service.py` | `CompareService.run`, `SideSpec`, `CompareError` |
| Codebase manifest | `src/db_project_manager/infrastructure/config/codebase_manifest.py` | `dbpm.manifest.json` |
| Reverse-engineer | `src/db_project_manager/application/reverse_engineer.py` | пишет `dbpm.manifest.json` |
| Adapter contract | `src/db_project_manager/infrastructure/database/base.py` | 8 abstractmethods (+ `get_table_row_counts`) |
| CLI | `src/db_project_manager/presentation/cli/main.py` | subapps: `graph`, `deploy`, `compare` |
| GUI actions | `src/db_project_manager/presentation/gui/actions/` | 4 действия: reverse_engineer, deploy_validate, graph_prepare, **compare** |
| GUI dialogs | `src/db_project_manager/presentation/gui/actions/dialogs.py` | `CompareDialog`; `_connections_combo(allow_empty=)`, `CONNECTION_EMPTY_LABEL` |

---

## Test status

```bash
unset SSL_CERT_FILE REQUESTS_CA_BUNDLE CURL_CA_BUNDLE && uv run pytest tests/unit/ -q
# 380 passed (307 baseline + 62 CLI + 11 GUI follow-up)
uv run ruff check src/ tests/
# All checks passed!
```

GUI follow-up тесты: +8 в `test_action_cli.py` (build_cli_compare + CliRunner-контракт),
+3 в `test_action_panel_smoke.py` (кнопки последние, worker error, плейсхолдер combo),
правка `test_expected_actions_present` → 4 действия.

Ручной тест пользователем — пройден (отчёт `source.json`/`target.json`/`diff_report.json`
сформирован). Замечание по XOR закрыто hotfix'ом `1d261db`.

---

## Known gaps / NOT done (актуальные)

- **Overload resolution** in edge detection — Phase 8 (BACKLOG P1, не начата).
- **Edge diff** (compare graph edges) — BACKLOG P2.
- **Markdown-отчёт** compare — BACKLOG P3.
- **Валидация XOR в диалоге** (disable полей) — BACKLOG P3.
- **Настраиваемый фильтр типов объектов** compare (`DIFFED_TYPES`) — BACKLOG P3.
- Bare refs в function/view bodies → возможен false-positive `changed` (урок §35); mitigation: `qualify-refs`.

---

## Key decisions & constraints (без изменений + новое)

- **db_type compatibility**: compare только PG↔PG, GP↔GP (hard error иначе); тип в `dbpm.manifest.json`.
- **sqlglot~=27.0.0**: нормализация SQL через AST.
- **Compare GUI**: 4 поля с неявным XOR; `required_fields=("output_dir",)` (обходит баг `_apply_defaults`); после выполнения — открыть отчёт + сводка.
- **Плейсхолдер combo** `(каталог вместо подключения)`: позволяет выбрать каталог, не нарушая XOR, когда в `store` есть подключения.

---

## Where to read more

| Doc | Why |
|-----|-----|
| `_phases_/Phase_09.md` | Свод фазы (CLI + GUI follow-up) |
| `_tasks_/phase_09/002_result_phase_09.md` | CLI-результат |
| `_tasks_/2026-07-29/20260729_003_compare_gui_action_result.md` | GUI-результат + hotfix |
| `_tasks_/2026-07-29/20260729_002_compare_gui_action_final.md` | GUI-норматив |
| `LESSONS_LEARNED.md` §44-46 | Уроки Phase 9 |

---

## Next planned work

**Phase 8 — Overload resolution in edge detection** (BACKLOG P1):
`_build_names_index` индексирует по bare `object_name`; вызовы перегруженных функций
ведут к первой попавшейся вершине. MVP: литералы + колонки с известным типом;
неразрешимые вызовы — как сейчас + warning. Чеклист — `LESSONS_LEARNED.md`
«Контрольный список для Phase 8».
