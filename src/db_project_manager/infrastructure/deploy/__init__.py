"""Controlled-deployment infrastructure (Phase 10+).

Package for everything that owns the deploy mechanic: the service schema DDL,
canonical validators, and (Phase 10 S7) the pre/post runner. Domain models
live in ``db_project_manager.domain.deploy`` — this package is the I/O side.
"""
