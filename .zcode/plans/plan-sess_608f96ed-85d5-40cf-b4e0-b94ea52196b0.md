# Phase 12 — S9: закрытие фазы (документы + регрессия)

Код фазы завершён и зелёный: unit **846 passed**, integration **24 passed**, ruff чистый. S9 — документальное закрытие по конвенциям (TASK_CONVENTIONS §6: каждый документ — отдельный коммит; код и docs не смешиваются). Изменений кода не планируется.

## Шаги

1. **Финальная регрессия** (уже зелёная, подтвердить): `pytest tests/unit/ -q`, `ruff check`, `-m integration` — результаты зафиксировать в документах (846 / 24 / ruff clean).

2. **README.md** → коммит `docs(readme): deploy plan/apply — Phase 12`:
   - статус-строка: «Phase 12 — ALTER + Delta готов … Post-deploy/отчёты — Phase 13»;
   - в CLI-блок после `deploy analyze`: `deploy plan` (dry-run: gate + классификация safe/needs-pre/blocked + артефакты `delta/`, `plan.json/md`; exit 0/1/2) и `deploy apply` (мутирующая: репетиция на temp-аналоге → pre → CD-11 → дельта → post → версия `source='apply'`; флаги `--include-drops/--no-rehearsal/--keep-rehearsal-db`; seed `__migrations/seed/` только в репетиции; восстановление повторным apply).

3. **ROADMAP.md** → коммит `docs(tasks): ROADMAP — Phase 12 done`: §2 строка «шаг 4 Phase 12» → ✅ done (коммиты `d76d804`…`f55b5a7`); примечание в §6, что prerequisite AI-трека (column-diff) закрыт.

4. **BACKLOG.md** → коммит `docs(tasks): BACKLOG — пункты по итогам Phase 12`, три записи:
   - P3 «Multi-statement normalize + comment-level diff» (из ограничения S2: `normalize_sql` берёт первый statement → `COMMENT ON` не в хэше; менять хэш нельзя — нужен переход);
   - P3 «Авто-генератор seed "одной записи"» (ALT-8b поверх ручных seed-скриптов);
   - P3 «`deploy analyze`: даунгрейд safe-alter нарушений по ALT-3» (standalone analyze остался table-level строгим — расхождение с plan/apply, где есть даунгрейд по классификации).

5. **LESSONS_LEARNED.md** → коммит `docs(lessons): §50-§52 — уроки Phase 12`:
   - §50: sqlglot `NotNullColumnConstraint(allow_null=True)` для явного `NULL`-маркера RE-DDL — имя класса врёт; проверять семантику на живом выводе;
   - §51: catalog-сегмент `object_key` ломает сравнение между средами/репетицией — identity не должна включать имя БД;
   - §52 (продолжение §49): один падающий e2e-сценарий = стек причин (NULL-маркер → имена `__deploy` → catalog) — фиксы по слоям; + design-нота: табличный gate Phase 11 vs колоночная классификация ALT-3 разрешены residual-даунгрейдом.

6. **`_tasks_/phase_12/Phase_12_result.md`** → коммит `docs(tasks): phase_12 result`: по шаблону Phase 11 — «Что сделано» (таблица Шаг|Коммит|Что: S1-S7 + 3 фикса S8 + e2e), «Отклонения от плана» (S6: reuse `DeployValidateService.run(keep_db=True)` вместо рефактора `deploy_into_new_db`; S8: gate-residual даунгрейд и plan-режим с BLOCKED-артефактами вместо падения — вне исходного плана; README «Возможности» не расширялся), «Известные ограничения», «Проверки», чеклист уроков, «Новые уроки», «Где читать дальше».

7. **`_phases_/Phase_12.md`** → коммит `docs(phase_12): ALTER + Delta — свод фазы`: ~80-100 строк по PHASES_CONVENTION — цель (CD-ALT-1..4, CD-11..15), «Что сделано» одной таблицей по слоям (`domain/delta.py`, `diff/columns.py`, comparator, `deploy/alter_plan.py`+`plan_report.py`, `application/{delta_service,deploy_apply_service}.py`, CLI), ключевые решения (ALT-1b SQL-source-of-truth, ALT-5 репетиция, матрица ALT-3, gate-residual даунгрейд, catalog-insensitive identity), проверки, NOT done, ссылки.

8. **`_checkpoints_/20260816_001_checkpoint.md`** → коммит `docs(checkpoint): add 20260816_001 — Phase 12 done`: 10 разделов по шаблону 20260814_002 (delta относительно 20260815_001, architecture map дополнений, известные пробелы, test status, next = Phase 13 Post-deploy+отчёты / опционально AI-track).

## Результат
8 коммитов (7 docs + подтверждение регрессии), Phase 12 полностью закрыта по циклу draft→final→plan→S1..S9, проект готов к Phase 13.